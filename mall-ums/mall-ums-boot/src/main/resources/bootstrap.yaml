spring:
  main:
    # 非web的方式运行
    web-application-type: none
    allow-circular-references: true
    allow-bean-definition-overriding: true
  application:
    name: mall-ums
  profiles:
    active: dev